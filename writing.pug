---
layout: base.pug
---

ul.writing.fade-in.fade-in--fast
  - let year = 0;
  - let change = false;

  each post in collections.posts.reverse()
    //- -console.log(post.data.date)
    li.writing__item
      a(href=post.url)=post.data.title
      //- span.writing__item-year!= full_date(post.date, config.date_format)


    //- - const postYear = date(post.date, 'YYYY') - 0;
    //- - change = year !== postYear;
    //- - year = change ? postYear : year;
    //- if change
    //-   span.writing__year=year